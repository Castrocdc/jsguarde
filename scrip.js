const laHora = 300
function Alumno (nombre, apellido, edad, pagoCuota){
    this.nombre = nombre;
    this.apellido = apellido;
    this.edad = edad;
    this.pagoCuota = pagoCuota;
  }
let array = [alumnos]
console.log (array)
function cuidado(laHora) {
  let guarderia = prompt ("ingrese horas de cuidado por dia")

   guarderia = guarderia * laHora * 20
    alert ("su cuota es de " + guarderia + " pesos al mes")

       
}
niño = prompt ("ingrese la edad del niñ@")

if (niño <= 5){

 cuidado (laHora)
 
}
else alert ("el niñ@ es mayor a lo requerido")

Alumno (prompt ("ingrese nombre:"), prompt("ingrese apellido:"), prompt("ingrese edad:"), prompt("tiene la untima cuota paga?:"));

if (this.pagoCuota == "si"){
  alert ("aprobado")
} else {
  alert("no pago la cuota")
} 
